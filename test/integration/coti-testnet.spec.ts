describe("COTI testnet encrypted CPMM integration", function () {
  it.skip(
    "requires two funded LP wallets, a separate MPC-call probe identity, onboarded AES keys, and two deployed PrivateERC20 test tokens; run the documented harness instead of fabricating plaintext mocks",
  );
});
